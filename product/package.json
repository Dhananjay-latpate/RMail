{
  "name": "rmail-product",
  "private": true,
  "workspaces": [
    "api",
    "portal",
    "webmail"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=api & npm run dev --workspace=portal & npm run dev --workspace=webmail",
    "build": "npm run build --workspaces",
    "db:migrate": "npm run db:migrate --workspace=api",
    "db:generate": "npm run db:generate --workspace=api"
  }
}
